'use client'
import { simulationMeta } from '@/data/simulation-meta'
import SectionRenderer from './SectionRenderer'

export default function SimulationMeta() {
  return <SectionRenderer section={simulationMeta} />
}
